<div class='p-32 bg-gradient-to-r from-indigo-500 h-screen w-screen flex flex-row items-center'>
  <div class='w-full flex flex-row items-stretch h-full border-2 border-white bg-transparent rounded-lg'>
    <div class= 'backdrop-filter backdrop-blur-sm bg-white/10 w-2/3 h-full flex justify-center items-center flex-col gap-y-5'>
    <h2 class='text-3xl text-white font-bold'>Log in</h2>

    <%= simple_form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
      <div class="flex flex-col gap-y-5">
        <%= f.input :email,
                    required: false,
                    autofocus: true,
                    input_html: { autocomplete: "email", class: 'border-white rounded-lg bg-transparent ml-5'} %>
        <%= f.input :password,
                    required: false,
                    input_html: { autocomplete: "current-password", class: 'border-white rounded-lg bg-transparent ml-5'} %>
        <%= f.input :remember_me, input_html: {class: 'border-white rounded-lg mr-4'}, as: :boolean if devise_mapping.rememberable? %>
      </div>

      <div class="bg-white rounded-lg mt-5 p-3 text-purple flex justify-center">
        <%= f.button :submit, "Log in" %>
      </div>
    <% end %>

    <%= render "devise/shared/links" %>
    </div>
    <div class='border-2 border-white bg-white w-1/3 h-full'>
      <img src='https://img.freepik.com/premium-vector/purple-lock-icon-with-cloud-flat-style-security-padlock-protection-concept-isolated-white_668430-328.jpg' alt='padlock' />
    </div>
  </div>
</div>
